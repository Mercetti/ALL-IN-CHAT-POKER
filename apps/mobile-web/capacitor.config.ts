import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.merce.aceycontrol',
  appName: 'Acey Control Center',
  webDir: 'dist'
};

export default config;
